services:
  archonguard:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    profiles: ["redis"]
